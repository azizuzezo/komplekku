# Nixpacks configuration for Railway deployment
# Disable automatic Prisma build secret requirement

[variables]
NIXPACKS_NO_PRISMA = "1"
DATABASE_URL = "postgresql://komplekku:komplekku@localhost:5432/komplekku"
